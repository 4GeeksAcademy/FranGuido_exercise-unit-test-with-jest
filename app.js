// this is a function that sums two numbers
const sum = (a, b) =>{
    return a + b
}

// console log to see the result
console.log(sum(7,3))

// Exporting function (will be used in another file)
module.exports = {sum};